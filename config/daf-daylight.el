(require 'daylight)

(setq daylight-morning-theme 'adwaita)
(setq daylight-afternoon-theme 'solarized-light)
(setq daylight-evening-theme 'twilight)
(setq daylight-late-theme 'solarized-dark)

(daylight-run)

(provide 'daf-daylight)
